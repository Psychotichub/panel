# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/
DB_NAME=daily_report_system

# JWT Authentication
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=30d

# Cookie Configuration
COOKIE_MAX_AGE_DAYS=30

# JWT Authentication
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=30d

# Cookie Configuration
COOKIE_MAX_AGE_DAYS=30

# Manager Account Creation (for createManagerAccount.js script)
# Use MANAGER_* prefix to avoid conflicts with OS environment variables
MANAGER_USERNAME=
MANAGER_PASSWORD=
MANAGER_EMAIL=
MANAGER_COMPANY=


# Alternative: Use unprefixed variables (may conflict with OS USERNAME on Windows)
# USERNAME=admin
# PASSWORD=change_this_password
# EMAIL=
# COMPANY=